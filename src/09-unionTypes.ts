(()=>{
  let userId: string | number;
userId = 450;
userId = 'Facundo';

function greeting(myText: string | number){ //le paso que puede ser string o number según corresponda

}

greeting('hola');
greeting(45);

})();
